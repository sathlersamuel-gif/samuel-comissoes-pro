(function(){'use strict';const KEY='scp_guardian_runtime_errors';function clean(){try{const list=JSON.parse(localStorage.getItem(KEY)||'[]');if(!Array.isArray(list))return;const kept=list.filter(e=>{const m=String(e?.message||'').toLowerCase();const offline=m.includes('client is offline')||m.includes('failed to get document because the client is offline')||m.includes('network request failed');return !offline;});if(kept.length!==list.length)localStorage.setItem(KEY,JSON.stringify(kept));}catch(_){}}clean();window.addEventListener('online',clean,{passive:true});document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')clean();});document.addEventListener('click',e=>{if(e.target?.closest?.('#guardiaDiagnostico,#guardianFullAudit,#guardianAutoFix,#guardianCopyReport'))clean();},true);setInterval(clean,60000);window.SCPGuardianOfflineFilter={clean};})();